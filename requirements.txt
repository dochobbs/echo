# Echo - AI Attending Tutor Service
# Pinned to compatible minor versions (~=) for reproducible builds.

# --- Core web framework ---
fastapi~=0.128.0
uvicorn[standard]~=0.40.0
pydantic~=2.12.0
pydantic-settings~=2.12.0
python-dotenv~=1.2.0
python-multipart~=0.0.22

# --- LLM ---
anthropic~=0.75.0

# --- Voice ---
elevenlabs~=2.27.0
deepgram-sdk~=3.0
# openai-whisper>=20231117  # Optional: local Whisper fallback for STT

# --- WebSocket ---
websockets~=15.0

# --- Database / auth ---
supabase~=2.27.0
supabase-auth~=2.27.0
sqlalchemy~=2.0.0
psycopg2-binary~=2.9
bcrypt~=5.0
pyjwt~=2.10.0
email-validator~=2.3.0

# --- Utilities ---
httpx~=0.28.0
pyyaml~=6.0
